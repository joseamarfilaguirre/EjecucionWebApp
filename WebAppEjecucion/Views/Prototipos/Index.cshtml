@model IEnumerable<WebAppEjecucion.Models.Prototipos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Prototipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Prototipo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPrototipo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPrototipo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPrototipo })
        </td>
    </tr>
}

</table>
