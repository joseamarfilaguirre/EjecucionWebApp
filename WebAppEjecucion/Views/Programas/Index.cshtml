@model IEnumerable<WebAppEjecucion.Models.Programa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Programa1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Programa1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPrograma }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPrograma }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPrograma })
        </td>
    </tr>
}

</table>
