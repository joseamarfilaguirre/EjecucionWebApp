@model IEnumerable<WebAppEjecucion.Models.EmpresaConstructora>

@{
    ViewBag.Title = "Index";
}

<h2>Empresas Constructoras</h2>

<p>
    @Html.ActionLink("Agregar Empresa", "Create",null,null, new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            Nombre de Empresa
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmpresaConstructora1)
        </td>
        <td>
            @Html.ActionLink("Ver Detalles", "Details", new { id = item.IdEmpConstructora }, new { @class = "btn btn-primary" }) |
            @Html.ActionLink("Editar", "Edit", new { id=item.IdEmpConstructora }, new { @class = "btn btn-warning" }) |            
            @Html.ActionLink("Borrar", "Delete", new { id=item.IdEmpConstructora }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
