@model IEnumerable<WebAppEjecucion.Models.EmpresaConstructora>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmpresaConstructora1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmpresaConstructora1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEmpConstructora }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEmpConstructora }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEmpConstructora })
        </td>
    </tr>
}

</table>
